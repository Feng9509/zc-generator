server:
  port: 8888

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.1.171:3306/idm_test_zjyh_person?useUnicode\=true&characterEncoding\=utf8&zeroDateTimeBehavior=convertToNull
    username: root
    password: rootpwd_commnet

  mvc:
    view:
      # 页面默认前缀目录
      prefix: /WEB-INF/page/
      # 响应页面默认后缀
      suffix: .jsp
    # 静态资源路径
    static-path-pattern: /static/**

mybatis:
  mapper-locations: classpath:mapper/*.xml
